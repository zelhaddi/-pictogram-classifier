#ifndef NUMBER_PAGE_H
#define NUMBER_PAGE_H

/** Find the number page in the image
 * @param gray_image : the image in grayscale
 * @param show_image : if you want to show the image and draw things on it
 * @return the number page
 */
int find_number_page(Mat& gray_image, bool show_image);


#endif /*NUMBER_PAGE_H*/
